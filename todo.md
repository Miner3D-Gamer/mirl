# TODO in Mirl:

## Helper scripts:

Create:

- If a flag exists and a dependency has the same flag, that flag needs to be included
- Generate all relevant combination of flags and test if each one compiles
- Checks all README.md if they are in the correct order and if all points are present

## Crates

- Implement `mirl_geometry` and uncomment all code in the other libs that depend on it
- dissolve `mirl_consts` into `mirl_extensions_math` and extend it with length units and temperature
- Improve the `RawCursor` to `.ico` formatting experience
- Check if glfw backend renderers red and blue color channels are swapped
- Check if all `README.md` are up to date quality wise

## Testing

- Test the cursors system more extensively
- Write tests for every lib

## Other

There are over 100 todo in this workspace, work them out
